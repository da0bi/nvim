require('lualine').setup({
    options = {theme = 'gruvbox', section_separators = '', component_separators = ''}
})

--[[
-- if vscode colortheme is installed one can use it for lualine 
require('lualine').setup({
    options = {theme = 'vscode', section_separators = '', component_separators = ''}
})
--]]
